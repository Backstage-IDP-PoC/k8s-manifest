apiVersion: v1
kind: Service
metadata:
  name: dssp-dev
spec:
  selector:
    app: dssp-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dssp-dev-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dssp-dev.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dssp-dev
#             port:
#               number: 80