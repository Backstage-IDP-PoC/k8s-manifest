apiVersion: v1
kind: Service
metadata:
  name: test-central-sonar-vault-01
spec:
  selector:
    app: test-central-sonar-vault-01
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer