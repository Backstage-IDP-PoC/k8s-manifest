apiVersion: v1
kind: Service
metadata:
  name: test-central-sonar-vault-01
spec:
  selector:
    app: test-central-sonar-vault-01
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: test-central-sonar-vault-01-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: test-central-sonar-vault-01.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: test-central-sonar-vault-01
#             port:
#               number: 80