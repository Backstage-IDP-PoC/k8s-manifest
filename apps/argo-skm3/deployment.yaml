apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-skm3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argo-skm3
  template:
    metadata:
      labels:
        app: argo-skm3
    spec:
      containers:
        - name: argo-skm3
          image: sureshmavp1/argo-skm3:1.0.1
          imagePullPolicy: Always
          # ports:
          #   - containerPort: 3000