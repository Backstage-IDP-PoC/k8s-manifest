apiVersion: v1
kind: Service
metadata:
  name: 
  namespace: 
  labels:
    app: 
    backstage.io/component: 
spec:
  type: ClusterIP          # ← Never LoadBalancer; LB is shared
  selector:
    app: 
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
