apiVersion: v1
kind: Service
metadata:
  name: vault-demo-01
spec:
  selector:
    app: vault-demo-01
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: vault-demo-01-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: vault-demo-01.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: vault-demo-01
#             port:
#               number: 80