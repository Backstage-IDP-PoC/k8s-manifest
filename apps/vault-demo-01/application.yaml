apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-demo-01
  namespace: argocd
  labels:
    app.kubernetes.io/name: vault-demo-01
spec:
  project: default
  source:
    repoURL: https://github.com/Backstage-IDP-PoC/k8s-manifest.git
    targetRevision: main
    path: apps/vault-demo-01
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
